const express = require('express');
const KioskController = require('../controllers/KioskController');
const router = express.Router();

// router.get("/new-ticket", [KioskController.createNewTicket]);

module.exports = router;
